# Repository instructions

Claude Code reads `CLAUDE.md`; Codex reads `AGENTS.md`. The instructions
themselves are not harness-specific, so they live in one file and this one
imports it rather than restating it. Edit `AGENTS.md`.

@AGENTS.md
