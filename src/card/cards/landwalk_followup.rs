//! Stable definition IDs appended by the follow-up sweep over identities whose
//! audit lines still named landwalk as their gap after the keyword landed.

use crate::CardDefinitionId;

pub const MERFOLK_ASSASSIN: CardDefinitionId = CardDefinitionId(1436);
pub const WORMWOOD_TREEFOLK: CardDefinitionId = CardDefinitionId(1437);
